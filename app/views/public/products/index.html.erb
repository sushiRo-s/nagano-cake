<!--商品のインスタンス変数admin側と確認する？-->
<h1>商品一覧<span>（全<%= @products.count %>件）</span></h1> 

<% @products.each do |product| %>
  <%= product.image %>
  <%= product.name %>
  <%= product.price %>
<% end %>

<!--kaminariつける-->
