<h1>配送先編集</h1>
<%= form_with model:@delivery, url:"/public/deliveries#{@delivery.id}", local:true do |f| %>
  <p>郵便番号（ハイフンなし）</p>
  <%= f.text_field :postcode %>
  <p>住所</p>
  <%= f.text_field :address %>
  <p>宛名</p>
  <%= f.text_field :name %>
  <%= f.submit '変更内容を保存' %>
<% end %>
