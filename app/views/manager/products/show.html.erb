<div class="container">
    <div class="row">
        <div class="col-lg-2"></div>
        <div class="col-lg-3">
        </br>
        <h2>商品詳細</h2>
         </br>
         <%= attachment_image_tag @product, :image, :fill, 200, 200, format: 'jpeg' %>
        </div>
        
        <div class="col-lg-7">
         </br></br></br>
           <p>商品名　　　<%= @product.name %></p>
           <p>商品説明　　　<%= @product.title %></p>
           <p>ジャンル名　　<%= @product.genre.name %></p>
           <p>税込み価格 (税抜き価格)　　<%= (@product.price*1.1).floor %> (<%= @product.price %>) 円</p>
           <p>販売ステータス　　<% if @product.is_active == true %>販売中
                              <% elsif @product.is_active == false %>販売停止中
                              <% else %>
                              <% end %>
            </br></br>
            <%= link_to ' 編集する', edit_manager_product_path(@product.id) , class: 'btn btn-success' %>
         </div>
     </div>
</div>